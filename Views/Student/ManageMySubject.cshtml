@{
    @model IEnumerable<Final_LitchiLearn.Models.Enrol>
    ViewData["Title"] = "Manage My Subjects";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

@*<div class="container p-3">
    <div class="row pt-4">
        <div class="col-5">
            <h2 class=align ="right" style="color :#ff8c69">Time Table</h2>
        </div>
        <div class="col-5 text-right">
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        Enrol ID
                    </th>
                    <th>
                        Student ID
                    </th>
                    <th>
                        Subject
                    </th>
                  
                </tr>
            </thead>
            <tbody>
                @foreach (var Enrol in Model)
                {
                    <tr style="padding:10px">

                        <td width="23%">@Enrol.EnrolID</td>
                        <td width="23%">@Enrol.StudentID</td>
                        <td width="23%">@Enrol.SubjectID</td>
                        <td width="23%">
                            <div class="w-10 btn-group" style="color :#ff8c69" role="group">
                                <a asp-controller="Student" asp-action="MySubjectPage" asp-route-ID="" class="btn btn-danger mx-1">Go to Subject</a>
                            </div>
                        </td>



                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> Error Please contact a member of the Admin staff</p>
    }
</div>*@


